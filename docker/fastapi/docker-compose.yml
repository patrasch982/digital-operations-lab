services:
  lab-api:
    build: .

    container_name: lab-api

    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "8000:8000"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro

    networks:
      - lab-network

networks:
  lab-network:
    external: true
